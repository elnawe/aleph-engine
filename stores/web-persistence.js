// Persistence is an object that takes care of all kind of saving to storage mechanics from the data stores.
// This object should be working together with data-store (core) but could be used outside with any kind of
// libraries or objects.
import { webStore } from 'stores/storage-types';

export default class WebPersistence {

}
